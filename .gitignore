# v3 per-session artifacts — entire runs tree (dumps, snapshots, scripts, verdicts, model outputs)
runs/
models/*.xlsx
!models/.gitkeep

# Benchmark ephemera (regenerable per run / per loop)
benchmarks/runs/
benchmarks/cache/
benchmarks/experiments/runs.sqlite
benchmarks/experiments/status.json
benchmarks/experiments/history.jsonl
benchmarks/experiments/autoresearch.pid
benchmarks/experiments/proposals/

# Secrets
.env

# Python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store

# Office sideload temp files
/tmp/Excel add-in *.xlsx

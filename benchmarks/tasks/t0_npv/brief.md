# Task: NPV of a cash flow series

Compute NPV of the cash flows at the given discount rate. Inputs are in
`B3` (rate), `B5:B9` (Y1-Y5), `B10` (Y0). Fill `B13` with a formula using
Excel's NPV function (NPV excludes Y0, so add it separately).

Write `results.json` next to `model.xlsx` mapping the semantic key `npv` to
the cell address `Calc!B13`.

# Task: build a monthly revenue build

Build a "Revenue Build" sheet with monthly units (annual × seasonality), price,
and revenue for all 12 months plus totals. Follow `inputs/assumptions.md` for
seasonality fractions and price. Units and revenue columns must be formulas
referencing `annual_units` and the seasonality column. Include a totals row.
Write `results.json`.
